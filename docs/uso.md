# Como Usar


1. Execute `npm start` para iniciar o servidor de desenvolvimento.
2. Abra `http://localhost:3000` no navegador.
3. Navegue pelas seções utilizando o menu lateral.

Para gerar uma versão de produção:

```bash
npm run build
```
